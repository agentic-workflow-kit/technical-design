# Design Document Template

This is an alias, not a second authoring template.

Use the canonical DDD design template at:

```text
../../../methodologies/ddd/templates/technical-design.md
```

Do not copy the full template body here. Keeping one canonical body prevents
source-of-truth drift between the methodology profile and skill-local files.

Compatibility markers for root deterministic checks:

- `handoff_contract: technical-design-handoff-v0`
- `## 1. Planner Handoff Summary`
